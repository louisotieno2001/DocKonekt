<!-- views/blogs.ejs -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <!-- SEO Meta Tags -->
    <meta name="description" content="Welcome to DocKonekt. Search for your hospital below or scroll down to pick a hospital.">
    <meta name="keywords" content="DocKonekt, hospital, health, search">
    <meta name="author" content="Futurion Labs">

    <!-- Open Graph Protocol for Social Media Sharing -->
    <meta property="og:title" content="DocKonekt">
    <meta property="og:description" content="Welcome to DocKonekt. Search for your hospital below or scroll down to pick a hospital.">
    <meta property="og:image" content="https://dockonekt.com/app/public/res/Picture4.png">
    <meta property="og:url" content="https://dockonekt.com">
    <meta property="og:type" content="website">

    <!-- Twitter Card for Twitter Sharing -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@louiso_chieng1">
    <meta name="twitter:title" content="DocKonekt">
    <meta name="twitter:description" content="Welcome to DocKonekt. Search for your hospital below or scroll down to pick a hospital.">
    <meta name="twitter:image" content="https://dockonekt.com/app/public/res/Picture4.png">
    <link rel="stylesheet" href="styles/index.css">
    <link rel="stylesheet" href="styles/small-devices.css">
    <link rel="icon" href="./res/Picture4.png" type="image/x-icon">
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <style>
        #area {
            padding-left: 10px;
            padding-right: 10px;
            height: max-content;
        }

        h2 {
            color: aliceblue;
            margin-top: 90px;
            background-color: #616161;
            border-radius: 8px;
            padding: 1em;
        }

        .back {
            color: aliceblue;
            background-color: #616161;
            border-radius: 8px;
            padding: 1em;
            font-family: sans-serif;
            font-size: larger;
            font-weight: bold;
        }

        .profile-pic {
            margin: 1em;
            display: flex;
            justify-content: center;
            cursor: pointer;
        }

        .profile-pic img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background-position: center;
            background-size: cover;
            padding: 2em;
        }

        .info {
            background-color: rgb(34, 36, 51);
            margin: 10px;
            height: 100vh;
            font-family: sans-serif;
            color: aliceblue;
        }

        .uline {
            height: 1px;
            background-color: antiquewhite;
        }

        .name {
            display: flex;
            justify-content: center;
            padding: .5em;
        }

        .phone {
            display: flex;
            justify-content: center;
            padding: .5em;
        }

        .mail {
            display: flex;
            justify-content: center;
            padding: .5em;
        }

        span {
            margin-top: .099em;
            padding: .8em;
            cursor: pointer;
        }

        .detail {
            margin-top: .5em;
            padding: .8em;
        }
    </style>
</head>

<body>
    <div class="top_bar">
        <img src="./res/Picture4.png" alt="logo" class="logo">
        <ul class="menu-items" id="menu-items">
            <li><a href="home.html" title="Home page">Home</a></li>
            <li><a href="/blogs" title="Blogs page">Health Blog</a></li>
            <li><a href="about_us.html" title="Learn more about us">About Us</a></li>
            <li><a href="get_in_touch.html" title="Send uus a messaage">Contact Us</a></li>
            <li><a href="terms.html" title="Look at our terms of service">Terms of service</a></li>
            <li><a href="user-type.html" title="Register your pharmacy with us">My Pharma</a></li>
        </ul>
        <img src="./res/menu_icon.png" alt="menu_icon" class="menu_icon" id="menu_icon">
        <div class="profile">
            <img src="./res/profile.jpg" alt="Profile image">
        </div>
    </div>
    <div class="blogtitle"></div>
    <div class="info">
        <div class="profile-pic" id="profile-pic">
            <img src="./res/profile.jpg" alt="Profile image" title="Change profile picture" id="placeholder">
        </div>
        <div class="uline"></div>
        <div class="name">
            <h3>Name: </h3>
            <div class="detail" id="nameDetail">
                <%= name %>
            </div>
            <form id="editNameForm" style="display: none;">
                <input type="text" id="nameInput" value="<%= name %>">
                <button type="submit" id="submitName" class="button-65">Save</button>
            </form>
            <span class="material-symbols-outlined" id="edit-name" title="Edit name">edit</span>
        </div>
        <div class="phone">
            <h3>Phone: </h3>
            <div class="detail" id="phoneDetail">
                <%= phone %>
            </div>
            <form style="display: none;" id="editPhoneForm">
                <input type="text" id="phoneInput" value="<%= phone %>">
                <button type="submit" id="submitPhone" class="button-65">Save</button>
            </form>
            <span class="material-symbols-outlined" id="edit-phone" title="Edit phone number">edit</span>
        </div>

        <!-- <div class="uline"></div> -->
        <div class="mail">
            <h3>Email: </h3>
            <div class="detail">
                <%= email %>
            </div>
            <span class="material-symbols-outlined" title="Not editable">
                edit_off
            </span>
        </div>
        <!-- <div class="uline"></div> -->
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const menuIcon = document.getElementById('menu_icon');
            const menuItems = document.getElementById('menu-items');

            menuIcon.addEventListener('click', function () {
                menuItems.classList.toggle('show');
            });

            const editName = document.getElementById('edit-name');
            const editPhone = document.getElementById('edit-phone');
            const submitName = document.getElementById('submitName');
            const submitPhone = document.getElementById('submitPhone');
            const nameInput = document.getElementById('nameInput');
            const phoneInput = document.getElementById('phoneInput');
            const profImage = document.getElementById('placeholder');
            const profile = document.getElementById('profile-pic');

            profile.addEventListener('click', () => {
                
            });

            // Event listeners for edit buttons
            editName.addEventListener('click', async (e) => {
                e.preventDefault();
                nameDetail.style.display = 'none';
                editNameForm.style.display = 'block';
            });

            editPhone.addEventListener('click', async (e) => {
                e.preventDefault();
                phoneDetail.style.display = 'none';
                editPhoneForm.style.display = 'block';
            });

            submitName.addEventListener('click', async (e) => {
                e.preventDefault();

                const nameValue = nameInput.value.trim();

                if (nameValue === "") {
                    alert("The field is required")
                } else {
                    try {

                        let newName = {
                            newName: nameValue
                        };

                        const res = await fetch('/edit-name', {
                            method: "POST",
                            headers: {
                                'Content-Type': 'application/json'
                            },
                            body: JSON.stringify(newName)
                        });

                        const response = await res.json();

                        if (response.success) { // Check for the success property
                            alert("Update successful!");
                            location.reload();
                            nameDetail.style.display = 'block';
                            editNameForm.style.display = 'none';
                        } else {
                            alert("Update failed. Please try again.");
                        }
                    } catch (error) {
                        console.error("Error during registration:", error);
                        alert("An error occurred. Please try again later.");
                    }
                }

            })

            submitPhone.addEventListener('click', async (e) => {
                e.preventDefault();

                const phoneValue = phoneInput.value.trim();

                if (phoneValue === "") {
                    alert("The field is required")
                } else {
                    try {

                        let newPhone = {
                            newPhone: phoneValue
                        };

                        const res = await fetch('/edit-phone', {
                            method: "POST",
                            headers: {
                                'Content-Type': 'application/json'
                            },
                            body: JSON.stringify(newPhone)
                        });

                        const response = await res.json();

                        if (response.success) { // Check for the success property
                            alert("Update successful!");
                            location.reload();
                            nameDetail.style.display = 'block';
                            editNameForm.style.display = 'none';
                        } else {
                            alert("Update failed. Please try again.");
                        }
                    } catch (error) {
                        console.error("Error during registration:", error);
                        alert("An error occurred. Please try again later.");
                    }
                }

            })

        });
    </script>
</body>

</html>